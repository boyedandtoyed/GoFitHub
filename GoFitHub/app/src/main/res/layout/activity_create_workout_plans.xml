<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center"
    android:background="@android:color/white">

    <!-- Title of the Page -->
    <TextView
        android:id="@+id/titleText"
        android:text="Add YouTube Workout Video Link"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"/>

    <!-- EditText for entering YouTube link -->
    <EditText
        android:id="@+id/youtubeLinkEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter YouTube Link"
        android:inputType="textUri"
        android:layout_marginBottom="16dp"
        android:padding="10dp"
        android:background="@android:drawable/edit_text"/>

    <!-- Button to submit the YouTube link -->
    <Button
        android:id="@+id/submitButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Add Video Link"
        android:textColor="@android:color/white"
        android:background="@color/button_color"
        android:layout_marginBottom="16dp"
        android:padding="10dp"/>

</LinearLayout>
